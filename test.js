const arr = [1,5,3,7,2,4]
/* const result = arr.reduce((array, number) => {
    let index = 0;
    while (index <array.length && number < array[index]) index++;
    array.splice(index, 0, number);
    return array
    while(array.length > 0 ) {
        for( let i = 0; i < array.length; i++) {

        }
    }
    return 
}, []) */
const string = 'chao mwng den voi amela'
/* result = string.split(' ').map((str) => {
    return str[0].toUpperCase() + str.toLowerCase().slice(1)
}) */

result = string.split(' ').reduce()
console.log(result.join(" "))